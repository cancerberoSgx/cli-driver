{
	"devDependencies": {
		"lerna": "^2.9.1"
	},
	"scripts": {
		"test": "lerna run test",
		"postinstall": "lerna bootstrap",
		"doc": "lerna run doc",
		"clean": "lerna run clean; rm -rf node_modules package-lock.json"
	}
}
