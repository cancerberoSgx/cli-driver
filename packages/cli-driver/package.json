{
  "name": "cli-driver",
  "version": "0.1.3",
  "description": "like webdriver but for the command line",
  "main": "lib/src/index.js",
  "types": "lib/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "npm run test-critical",
    "test-js": "tsc && node node_modules/jasmine/bin/jasmine.js --config=./spec/support/jasmine-critical.json",
    "test-critical": "ts-node -r tsconfig-paths/register node_modules/jasmine/bin/jasmine --config=./spec/support/jasmine-critical.json",
    "test-all": "ts-node -r tsconfig-paths/register node_modules/jasmine/bin/jasmine --config=./spec/support/jasmine-all.json",
    "test-critical-coverage-html-report": "rm -rf coverage; nyc --clean -r lcov -e .ts -x \"*Spec.ts\" ts-node -r tsconfig-paths/register node_modules/jasmine/bin/jasmine --config=./spec/support/jasmine-critical.json",
    "test-critical-coverage-report": "nyc --clean -r json -e .ts -x \"*Spec.ts\" ts-node -r tsconfig-paths/register node_modules/jasmine/bin/jasmine --config=./spec/support/jasmine-critical.json",
    "prepare": "tsc",
    "doc": "tsc && typedoc --options typedoc.ts",
    "clean": "find . -name \"node_modules\" -exec rm -rf '{}' + ; rm -rf  package-lock.json lib tmp* src/*.d.ts",
    "lint": "tslint src/**/*.ts",
    "lint-fix": "tslint --fix src/**/*.ts",
    "help": "node -p \"'\\nHelp on this package\\'s npm scripts:\\n'+JSON.stringify(JSON.parse(require('shelljs').cat('package.json').toString())['scripts-description'], null, 2)+'\\n'\""
  },
  "scripts-description": {
    "npm test": "run critical tests. By default, tests are run using ts-node. You can filter test to run but entering for example: 'npm test -- **/writeSpec.ts'",
    "npm run test-js": "run critical tests but instead using ts-node compiles them with tsc and runs compiled JavaScript code",
    "npm run build": "run critical tests compiled with tsc and running compiled JavaScript code",
    "npm run watch": "continuously run TypeScript compiler when changes on .ts files are detected",
    "npm run help": "Prints this help"
  },
  "files": [
    "lib",
    "types"
  ],
  "keywords": [],
  "author": "Sebastián Gurin",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^9.6.2",
    "ansi-escape-sequences": "^4.0.0",
    "node-keys": "^0.1.3",
    "node-pty": "git+https://github.com/cancerberoSgx/node-pty.git",
    "shelljs": "^0.8.1"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.6",
    "@types/shelljs": "^0.7.8",
    "jasmine": "^3.1.0",
    "nyc": "^11.6.0",
    "strip-ansi": "^4.0.0",
    "ts-node": "^5.0.1",
    "tsconfig-paths": "^3.2.0",
    "tslint": "^5.9.1",
    "tslint-config-standard": "^7.0.0",
    "typedoc": "^0.11.1",
    "typescript": "^2.8.1"
  },
  "readme": "../../README.MD"
}
