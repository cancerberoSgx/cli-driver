language: node_js
node_js:
  - "9"
scripts:
  - npm install yarn@1.6.0 codecov -g
  
after_success:
  - cd packages/cli-driver/ && npm run test-critical-coverage-report && codecov 
os:
  - linux
  - osx
cache:
  directories:
    - "node_modules"
    - $HOME/.npm