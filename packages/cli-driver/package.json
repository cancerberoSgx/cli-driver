{
  "name": "cli-driver",
  "version": "0.0.7",
  "description": "like webdriver but for the command line",
  "main": "lib/src/index.js",
  "types": "./src/typings/cli-driver.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "tslint src/**/*.ts",
    "lint-fix": "tslint --fix src/**/*.ts",
    "test": "tsc && jasmine",
    "test-no-build": "jasmine",
    "prepare": "tsc",
    "doc": "typedoc --mode modules --out ../../docs --includes docs/tutorials --mode file src/ ",
    "clean": "rm -rf node_modules package-lock.json lib"
  },
  "keywords": [],
  "author": "Sebastián Gurin",
  "license": "ISC",
  "dependencies": {
    "node-pty": "^0.7.4",
    "shelljs": "^0.8.1"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.6",
    "@types/node": "^9.6.2",
    "@types/shelljs": "^0.7.8",
    "jasmine": "^3.1.0",
    "tslint": "^5.9.1",
    "tslint-config-standard": "^7.0.0",
    "typedoc": "^0.11.1",
    "typescript": "^2.8.1"
  }
}
